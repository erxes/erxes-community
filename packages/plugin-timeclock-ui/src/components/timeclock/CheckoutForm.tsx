import React from 'react';
import { ITimeclock, ITimelog } from '../../types';

type Props = {
  activeTimeclocksPerUser: ITimeclock[];
  timelogsPerUser: ITimelog[];
};

function CheckoutForm(props: Props) {
  return <div>asdasd</div>;
}

export default CheckoutForm;

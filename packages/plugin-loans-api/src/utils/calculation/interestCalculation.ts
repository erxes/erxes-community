/**
 *
 * @param interestPercent interest percent of year
 * @param balanceAmount balance amount if not payed loan amount
 * @param beginDate begin calculation date
 * @param endDate end of calculation date
 */
function getInterestAmount(
  interestPercent: number,
  balanceAmount: number,
  beginDate: Date,
  endDate: Date
) {}

import React from 'react';
import { Link, withRouter } from 'react-router-dom';

function IntegrationConfigs(props: any) {
  return <div>Hello</div>;
}

export default withRouter<any>(IntegrationConfigs);
